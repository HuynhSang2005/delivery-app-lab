Decisions made:

- Keep `name` and `description` only in SKILL.md frontmatter; remove other top-level keys.
- Preserve multi-line description block values unchanged.
- Append findings to .sisyphus/notepads/skills-cleanup/* (append-only files) for traceability.
